spring:
  profiles: stage

props:
  appName: zcp-alertmanager-stage
  alertManager:
    baseUrl: ${ALERT_BASE_URI}
    configMap: ${ALERT_CONFIGMAP}
    namespace: ${ALERT_NAMESPACE}
  prometheus:
    baseUrl: ${PROMET_BASE_URI}
    configMap: ${PROMET_CONFIGMAP}
    namespace: ${ALERT_NAMESPACE}
  mariaDB:
    url: ${MARIADB_URI}
    id: ${MARIADB_ID}
    password: ${MARIADB_PWD}
